# Configs

Robot and motor parameter files will live here. Keep values that both host tools
and firmware need in machine-readable files, then generate C headers or Python
constants from them instead of copying limits by hand.
